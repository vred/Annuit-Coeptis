<div class="container">

<h1><%= @league.name %></h1>


<%= will_paginate(@portfolios) %>

<ul class="portfolios">
  <%= render @portfolios %>
</ul>

<%= will_paginate(@portfolios) %>
<div class="span3">
<% if @portfolio.id.nil? %>
    <%= form_for [@league, @portfolio] do |f| %>
        <%= f.submit "Join this League", class: "btn btn-primary btn-quarter" %>
    <% end %>
<% end %>
</div>
</div>
